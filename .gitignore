node_modules

# Build output (regenerated by `npm run build`)
dist
.next

# Local tool state (Wrangler/Miniflare, npm cache, install locks)
.wrangler
.sites-runtime

# Environment files with secrets — never commit real keys
.env
.env.local
.env.production
.env*.local
!.env.example
.vinext/
